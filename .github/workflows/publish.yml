name: Publish Static Site
description: 
run-name: Publish REF({{ inputs. }})

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.11
      - name: Build Static Site Content (React)
        run: |
          
          
      - name: Create Output Archive
        uses: actions/upload-artifact@v6
        with:
          name: github-pages
      - name: Publish Archive to GitHub Pages
        id: publish
        uses: actions/deploy-pages@4
        with:
          name: 

      - name: Report
        run: |
          echo Successfully Deployed to {{ steps.publish.output.page_url }}!
